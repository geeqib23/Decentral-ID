import React, { useContext } from 'react';
import { TransactionContext } from '../context/TransactionContext';

function ReqDetails({index,}) {
    const {userVReqList} = useContext(TransactionContext)
    return (
        <div>
            
        </div>
    );
}

export default ReqDetails;